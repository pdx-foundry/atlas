atlas_valid = {
    unlocks_agenda = atlas_agenda
    custom_tooltip = atlas_tooltip
    custom_tooltip_with_modifiers = atlas_tooltip_with_modifiers
    potential = { always = yes }
    possible = { always = yes }
    on_enabled = { }
    on_disabled = { }
    modifier = { }
    triggered_modifier = { potential = { always = yes } modifier = { } }
    ai_weight = { weight = 1 }
    tradition_swap = { name = atlas_swap trigger = { always = no } }
}
atlas_omitted = { }
atlas_repeated = {
    unlocks_agenda = atlas_first
    unlocks_agenda = atlas_second
}
atlas_malformed = {
    unlocks_agenda = "agenda
broken"
}
atlas_unknown = {
    unlocks_agenda = atlas_agenda
    this_is_an_unknown_field = { broken = yes }
}
